[package]
name = "services"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = { version = "0.7", features = ["postgres", "runtime-tokio", "macros", "chrono"] }
models = { path = "../models" }
axum = { version = "0.6", features = ["macros"] }
